spring:
  application:
    name: catalog-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
  profiles:
    active: mysql

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
